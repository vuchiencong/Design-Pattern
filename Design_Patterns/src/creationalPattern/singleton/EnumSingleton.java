package creationalPattern.singleton;

/**
 * Khi dùng enum thì các params chỉ được khởi tạo 1 lần duy nhất,
 * đây cũng là cách giúp bạn tạo ra Singleton instance.
 */
public enum EnumSingleton {
    INSTANCE;
}
